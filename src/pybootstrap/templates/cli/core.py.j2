"""{{ project_name }} - 핵심 비즈니스 로직"""


def run(name: str) -> str:
    """메인 로직

    Args:
        name: 인사할 대상
    
    Returns:
        인사 메시지 문자열
    """
    return f"Hello, {name}! 👋 from {{ project_name }}"